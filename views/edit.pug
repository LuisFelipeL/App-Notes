extends index

block body
  div(class="note-form-page")
    form(action=`/notes/${currentNote._id}` method="get")
      div
        textarea(id="title" name="title" placeholder="Escribe el título..." autocomplete="off" autofocus)= currentNote.title
      div
        textarea(id="body" name="body" rows="8" placeholder="Escribe el contenido del articulo...")= currentNote.body
      div(class="form-actions")
        a(href=`/notes/${currentNote._id}` class="btn") Cancelar
        button(type="submit" class="btn btn-primary" id="btn-update" data-id=`${currentNote._id}`) Actualizar nota

  script
    include ../public/js/edit.js