extends index

block body
  div(class="note-form-page")
    form(action="/notes" method="post")
      div
        input(type="text" id="title" name="title" placeholder="Escribe el título..." autocomplete="off" autofocus)
      div
        textarea(
          id="body" name="body" rows="8" placeholder="Escribe el contenido del articulo...")
      div(class="form-actions")
        a(href="/" class="btn")= "Cancelar"
        button(type="submit" class="btn btn-primary")= "Crear nota"

  script
    include ../../public/js/new.js